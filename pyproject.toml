[project]
name = "mcp_project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "crewai[tools]>=0.95.0",
    "httpx>=0.28.1",
    "mcp[cli]>=1.6.0",
]

[tool.uv.workspace]
members = [
    "tools",
]
